
  - console.log(arr.length); //свойство любого объекта
  - string.prototype.link(); //метод - выполняет действие над объектом (string) - их нужно гуглить
  
  const str = "teSt";

  const arr = [1, 2, 4];

  // console.log(str[2]); //свойство


  console.log(str.toLocaleUpperCase());
  console.log(str.toLowerCase());
  console.log(str);

  const fruit = 'Some fruit';
  console.log(fruit.indexOf('q'));


  const logg = 'Hello, world';

  console.log(logg.slice(7, 12));